var gulp    = require('gulp')
  , texsys  = require('./texsys')
  ;

function System() {
  this.args = this.args || null;
  this.cmd = this.cmd || null;
  this.cb = this.cb || null;
  this.child = 

sys = new System();
sys.run(

function system(options, cb) {
  options.args = options.args || [];
  options.child = 

gulp.task('default', [ 'lint', 'build' 'ugly' ]);
gulp.task('lint', function() {

